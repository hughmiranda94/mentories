import * as Ajax from '../public/src/utils/ajax';

describe('Ajax.load method', () => {
    it('Should exists', () => {
        
    });
    it('Should returs a resolved promise when the service is pulling succesfully', (done) => {
        
    });
    it('Should returns a rejected promise when the service is not pulling anything', (done) => {
        
    });
});
